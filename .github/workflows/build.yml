name: Injector_Final_Fix_v5
on: [push, workflow_dispatch]
jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Extract and Move Files
        run: |
          unzip -o "*.zip"
          # Makefile nerede olursa olsun bul ve her şeyi oraya taşı
          MAKEFILE_PATH=$(find . -name "makefile" -o -name "Makefile" | head -n 1)
          MAKEFILE_DIR=$(dirname "$MAKEFILE_PATH")
          cp -R "$MAKEFILE_DIR"/* . || true
          chmod -R 777 .

      - name: Setup Theos & SDKs
        run: |
          # Theos'u ana dizine kur
          export THEOS=$GITHUB_WORKSPACE/theos
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"
          # SDK'ları indir
          mkdir -p $THEOS/sdks
          curl -LO https://github.com/theos/sdks/archive/master.zip
          unzip -q master.zip -d $THEOS/sdks
          mv $THEOS/sdks/sdks-master/* $THEOS/sdks/ || true
          echo "THEOS=$THEOS" >> $GITHUB_ENV
          
      - name: Build Project
        run: |
          # Theos yolunu tanıt ve derle
          export THEOS=$GITHUB_WORKSPACE/theos
          make package FINALPACKAGE=1
          
      - name: Upload Result
        uses: actions/upload-artifact@v4
        with:
          name: S2-Injector-Success
          path: |
            packages/*.deb
            *.deb
            *.ipa
